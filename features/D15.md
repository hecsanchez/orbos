# TASK: Day 15 — iPad Evidence Capture Screen

You are a mobile engineering agent building Orbos. Read `context.md` and `CLAUDE.md` at the root before writing any code.

The evidence screen currently shows "Próximamente" alerts. It needs real camera/audio capture, local storage in SQLite, and sync to R2. The `AudioExplain` component already has a working audio recording pattern to reuse.

---

## Step 1 — Install Dependency

```bash
pnpm add expo-image-picker expo-file-system --filter @orbos/ipad
```

---

## Step 2 — Audio Recorder Hook

Create `apps/ipad/src/hooks/useAudioRecorder.ts`:
- Extract recording logic from `AudioExplain.tsx`: `startRecording`, `stopRecording`, `playRecording`, `resetRecording`, `cleanup`
- State: `isRecording`, `recordingUri`, `recordingDuration`, `isPlaying`, `permissionError`
- Uses `expo-av` Audio API, requests permissions, configures audio mode
- Duration tracking via `setInterval` (every 500ms)
- Playback via `Audio.Sound.createAsync` with status updates
- Accepts `minDuration` parameter (default 3 seconds)

---

## Step 3 — Rewrite Evidence Screen

Rewrite `apps/ipad/src/app/(session)/evidence.tsx`:
- Read current item from session context → get `phenomenon_id` from `currentItem`
- Two capture modes with a mode selector screen:
  - **Audio**: use `useAudioRecorder` hook, show pulse animation during recording, playback review, re-record option
  - **Photo**: use `expo-image-picker` `launchCameraAsync({ quality: 0.7 })`, show preview, retake option
- On submit: insert into `evidence_queue` SQLite table (file_uri, type, student_id, standard_id, phenomenon_id)
- "Listo" confirmation → `sessionEngine.completeCurrentItem()` + `sessionEngine.advance()`
- Age-adaptive theming via `useProfile()` context and `AGE_THEME`
- Back navigation between modes via "← Cambiar modo" button

---

## Step 4 — Update AudioExplain to Use Shared Hook

Modify `apps/ipad/src/components/interactions/AudioExplain.tsx`:
- Replace inline recording logic (~60 lines) with `useAudioRecorder` hook
- Maintain all existing UI and behavior (pulse animation, min duration, review phase)

---

## Step 5 — Implement Evidence Sync

Modify `apps/ipad/src/services/sync.service.ts`:
- Implement `flushEvidenceQueue()`:
  - Query `evidence_queue WHERE synced = 0`
  - For each: read file from `file_uri` via `expo-file-system`, create `FormData`, POST multipart to `/evidence/upload`
  - Mark `synced = 1` on success, `synced = -1` after 3 failures

---

## Step 6 — Update API Client

Modify `apps/ipad/src/services/api-client.ts`:
- Add `uploadEvidence(formData: FormData): Promise<EvidenceResponseDto>` — multipart POST (no Content-Type header — let fetch set boundary)
- Add `getPhenomenon(id: string): Promise<PhenomenonResponseDto>` — GET

---

## Step 7 — E2E Test

Create `e2e/ipad-e2e/evidence-capture.test.ts`:
- Test: evidence screen shows mode selector with audio and photo options
- Test: audio mode shows recording interface
- Test: audio recording starts and shows duration
- Test: stopping recording shows review phase
- Test: submit stores evidence and advances session

---

## Verification

- `pnpm exec nx run ipad:typecheck` passes
- Evidence screen: mode selector → audio recording with pulse → stop → review → submit
- Evidence screen: mode selector → photo capture → preview → submit
- Evidence appears in `evidence_queue` SQLite table
- Sync uploads evidence to R2 via multipart POST
