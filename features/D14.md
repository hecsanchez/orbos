# TASK: Day 14 — Phenomenon Designer Agent + Evidence Upload Pipeline

You are a backend engineering agent building Orbos. Read `context.md` and `CLAUDE.md` at the root before writing any code.

The orchestrator already checks for approved phenomena and slots them into daily plans, but nothing generates proposals yet. The evidence upload endpoint is a stub returning empty storage_url. Both need real implementations, plus several dashboard API endpoints for D16/D17.

---

## Step 1 — Install Dependencies

```bash
pnpm add @aws-sdk/client-s3 @aws-sdk/s3-request-presigner --filter @orbos/api
pnpm add -D @types/multer --filter @orbos/api
```

---

## Step 2 — Phenomenon Designer Agent

Create `apps/api/src/agents/phenomenon/phenomenon-designer.agent.ts`:

- Follow the `LessonDesignerAgent` pattern (`apps/api/src/agents/lessons/lesson-designer.agent.ts`)
- Inject: `LLMClient`, `PromptTemplateService`, `EmbeddingsService`, `SafetyAgent`
- Method `propose(studentId: string)`:
  1. Load student from DB (name, age, grade_target, interests)
  2. Query `masteryState` for unmastered standards (mastery_level < 0.8), join with `standards`
  3. Use RAG/embeddings to find related standards for context
  4. Build prompt via `PromptTemplateService.getTemplate('phenomenon_designer')`
  5. Call `LLMClient.generate()` → parse JSON array of 3 `PhenomenonProposal` objects
  6. Validate each with `validatePhenomenonProposal()` from `apps/api/src/phenomena/schemas/phenomenon-proposal.schema.ts`
  7. Run each through `SafetyAgent.checkPhenomenon()` (regen up to 3x on failure)
  8. Insert 3 rows into `phenomenonProposals` table (status: 'pending')
  9. Return array of `PhenomenonResponseDto`

Tests at `apps/api/src/agents/phenomenon/phenomenon-designer.agent.spec.ts`:
- Valid proposals inserted into DB
- Safety regen triggered on failure
- Validation errors trigger regen
- Student not found throws NotFoundException

---

## Step 3 — Seed Prompt Template

Create `apps/api/src/db/seeds/seed-prompts.ts`:
- Insert `phenomenon_designer` prompt template with variables: `{{student_name}}`, `{{student_age}}`, `{{grade}}`, `{{interests}}`, `{{unmastered_standards}}`, `{{related_standards_context}}`
- Run with `npx tsx apps/api/src/db/seeds/seed-prompts.ts`

---

## Step 4 — Wire Phenomena Controller

Modify `apps/api/src/phenomena/phenomena.controller.ts`:
- Inject `PhenomenonDesignerAgent`
- `POST /phenomena/propose`: call `agent.propose(dto.student_id)`, return proposals
- `PUT /phenomena/:id/approve`: Drizzle update → set status='approved', approved_by, approved_at
- `GET /phenomena/student/:studentId`: list proposals for a student
- `GET /phenomena/:id`: get single proposal by ID

Modify `apps/api/src/phenomena/phenomena.module.ts`:
- Import `AgentsModule`, register controller

---

## Step 5 — Evidence Upload Service

Create `apps/api/src/evidence/evidence.service.ts`:
- Init S3Client from `@aws-sdk/client-s3` using env vars `R2_ENDPOINT`, `R2_ACCESS_KEY`, `R2_SECRET_KEY`, `R2_BUCKET`
- `upload(file, dto)`: PutObject to R2 key `evidence/${student_id}/${phenomenon_id}/${uuid}.${ext}`, insert into `evidence` table, return `EvidenceResponseDto`
- `getSignedUrl(storageUrl)`: generate presigned GET URL
- `listByStudent(studentId)`: query evidence table
- `listByPhenomenon(phenomenonId)`: query evidence table

Modify `apps/api/src/evidence/evidence.controller.ts`:
- Inject `EvidenceService`
- `POST /evidence/upload`: use `@UseInterceptors(FileInterceptor('file'))` for multipart
- `GET /evidence/student/:studentId`: list evidence for student
- `GET /evidence/phenomenon/:phenomenonId`: list evidence for phenomenon

Modify `apps/api/src/evidence/evidence.module.ts` — add `EvidenceService` to providers

---

## Step 6 — Register Agent

Modify `apps/api/src/agents/agents.module.ts`:
- Add `PhenomenonDesignerAgent` to providers and exports

---

## Step 7 — Dashboard API Endpoints

Modify `apps/api/src/mastery/mastery.controller.ts`:
- `GET /mastery/student/:studentId`: return all mastery states for a student

Modify `apps/api/src/lessons/lessons.controller.ts`:
- `GET /lessons`: list lesson scripts (filter by admin_approved, standard_id)
- `PUT /lessons/:id/approve`: set admin_approved = true

Modify `apps/api/src/standards/standards.controller.ts`:
- `GET /standards`: list all standards (optional grade filter)

Create `apps/api/src/safety/safety-log.controller.ts`:
- `GET /safety/logs`: list safety logs with optional filters (passed, content_type)

Create `apps/api/src/safety/safety-log.module.ts` — register controller

Modify `apps/api/src/app/app.module.ts` — import `SafetyLogModule`

---

## Step 8 — Update Shared Types

Modify `packages/types/src/lib/types.ts`:
- Add `phenomenon_id?: string` to `PlanItemDto`
- Add `SafetyLogResponseDto` interface

---

## Verification

- `pnpm exec nx run api:typecheck` — no new errors from source files
- `pnpm exec vitest run --config apps/api/vitest.config.mts` — all 50 tests pass
- New agent tests: valid proposals, safety regen, validation regen, student not found
