You are a data engineering agent building the AI Learning OS. Read the attached context document (AI_LearningOS_Agent_Context_v1.3.md) before doing anything else.
You are also given two files from Day 1:

seeds/sep_standards.json — the parsed SEP standards
seeds/sep_parsing_report.md — the parsing report flagging gaps and ambiguities

Your job today is to validate and clean the JSON, finalize the standard ID schema, and produce a seed SQL file ready to run on Day 3 when the database is stood up.
Step 1 — Validate the JSON
Run the following checks against every object in sep_standards.json and produce a validation report:

id is unique across the entire file — flag any duplicates
id follows the format SEP-[SUBJECT]-[GRADE]-[UNIT].[STANDARD] exactly — flag any that don't
grade is an integer between 1 and 6
subject is one of: Matematicas, Espanol, Ciencias
description is non-empty and in Spanish
prerequisites only references IDs that exist in the same file — flag any dangling references
No standard has an empty id, description, or subject

For every failed check: log it, fix it if the fix is unambiguous (e.g. a formatting issue), or flag it for human review if the fix requires judgment.
Step 2 — Clean the JSON
Apply fixes where safe to do so automatically:

Normalize subject names to exactly: Matematicas, Espanol, Ciencias
Trim whitespace from all string fields
Ensure prerequisites is always an array, never null or missing
Remove any duplicate standards, keeping the first occurrence and logging the removal

Produce a final clean file: sep_standards_clean.json
Step 3 — Write the seed SQL
Write a file seed_standards.sql that inserts all standards from sep_standards_clean.json into the standards table. Use this schema:
sqlINSERT INTO standards (id, grade, subject, description, prerequisites)
VALUES (...)
ON CONFLICT (id) DO NOTHING;
The embedding column is NOT seeded here — that is handled on Day 4 when the RAG index is built. Do not include it in the seed.
Group inserts by grade and subject for readability. Add comments marking each group, e.g.:
sql-- Grade 1 — Matematicas
-- Grade 1 — Espanol
Step 4 — Produce a summary report
Write sep_validation_report.md containing:

Total standards before and after cleaning
List of every issue found: what it was, what was done (auto-fixed or flagged)
Final count breakdown: standards per subject per grade
Any items still requiring human review before Day 3

When you are done, confirm:

Final standard count in sep_standards_clean.json
Whether seed_standards.sql is ready to run as-is on Day 3
Any blockers or human review items that need resolution before the DB schema is built

Do not touch the database, build any API routes, or start any other task. Today is validation and seed prep only.
