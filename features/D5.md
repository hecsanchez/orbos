You are a backend engineering agent building Orbos. Read context.md and CLAUDE.md at the root before writing any code.
LLMClient, RAG index, embeddings, and all 10 interaction component schemas are complete. Your job today is to define the phenomenon output schema and flesh out all API routes with proper request/response DTOs — no stub returns, real validated shapes.

Step 1 — Phenomenon Output Schema
The Phenomenon Designer Agent produces two distinct outputs. Document and enforce their schemas.
Create apps/api/src/phenomena/schemas/phenomenon-proposal.schema.ts:
typescriptexport interface FacilitationGuide {
overview: string;           // 1–3 sentence summary of the activity
duration_days: number;      // 3–5
daily_steps: {
day: number;
title: string;
instructions: string;     // markdown, for the teacher/guide
discussion_prompts: string[];
}[];
materials_needed: string[]; // physical materials the guide needs
success_indicators: string[]; // how the guide knows it went well
}

export interface EvidencePrompt {
instruction_text: string;   // shown to child on screen, max 80 words
tts_text: string;           // read aloud to child, max 80 words
capture_type: 'audio' | 'photo' | 'both';
}

export interface PhenomenonProposal {
title: string;
description: string;
duration_days: number;
linked_standards: string[];   // standard IDs
facilitation_guide: FacilitationGuide;
evidence_prompt: EvidencePrompt;
}
Create a Zod schema for runtime validation of PhenomenonProposal. Add it to the InteractionValidatorService or create a PhenomenonValidatorService — your call, but validation must be enforced before any proposal is stored in the DB.

Step 2 — DTOs for All Routes
Create request/response DTOs using NestJS + class-validator for every route. No more stub responses — every route must validate its input and return a typed response shape.
Create apps/api/src/students/dto/:
typescript// create-student.dto.ts
export class CreateStudentDto {
name: string;           // @IsString @MinLength(1)
age: number;            // @IsInt @Min(5) @Max(11)
grade_target: number;   // @IsInt @Min(1) @Max(6)
interests: string[];    // @IsArray @IsString({ each: true })
}

// student-response.dto.ts
export class StudentResponseDto {
id: string;
name: string;
age: number;
grade_target: number;
interests: string[];
}
Create DTOs for every domain following the same pattern:
Standards:

SearchStandardsDto — { query: string, grade?: number, subject?: string, topK?: number }
StandardResponseDto — matches the standards table shape minus the embedding vector

Lessons:

GenerateLessonDto — { standard_id: string, student_age: number }
LessonScriptResponseDto — { id: string, standard_id: string, script: unknown[], safety_approved: boolean }

Attempts:

LogAttemptDto — { student_id: string, standard_id: string, interaction_component: string, correct: boolean, time_spent_seconds: number, hint_used: boolean, source: 'lesson' | 'phenomenon' }
AttemptResponseDto

Mastery:

MasteryResponseDto — { student_id: string, standard_id: string, mastery_level: number, confidence_score: number, has_direct_lesson_attempt: boolean, recommendation: 'advance' | 'practice' | 'reteach' }

Phenomena:

ProposePhenomenonDto — { student_id: string }
ApprovePhenomenonDto — { approved_by: string }
PhenomenonResponseDto

Evidence:

UploadEvidenceDto — { student_id: string, standard_id: string, phenomenon_id: string, type: 'photo' | 'audio' }


Step 3 — Wire Routes to DTOs
Update every controller to use the DTOs from Step 2. Routes still return placeholder data — but the input validation must be real and working. Install:
bashpnpm add class-validator class-transformer zod -w
Enable global validation pipe in main.ts:
typescriptapp.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true }));

Step 4 — Orchestrator Route
This route was missing from the Day 3 skeleton. Add it now:

GET /orchestrator/daily-plan/:studentId — returns the ordered daily plan for today
OrchestratorPlanDto — { student_id: string, date: string, plan: { standard_id: string, type: 'lesson' | 'practice' | 'phenomenon_evidence', estimated_minutes: number }[] }

Still a stub return — the actual Orchestrator Agent is Day 14.

Step 5 — API Documentation
Set up Swagger:
bashpnpm add @nestjs/swagger -w
Wire it up in main.ts so GET /api/docs shows all routes with their DTOs. Decorate every DTO with @ApiProperty(). This will be useful for the iOS and web agents who need to know the API shape.

When you are done, confirm:

POST /students with invalid age (e.g. age: 3) returns a 400 with a validation error
POST /attempts/log with source: 'invalid' returns a 400
GET /api/docs loads Swagger UI showing all routes
All Vitest tests pass:

bashpnpm exec nx run api:test

List all routes to confirm nothing is missing:

bashpnpm exec nx serve api
curl http://localhost:3000/api/docs-json | jq '.paths | keys'
Do not implement any agent logic today. That starts Day 8. Today is DTOs, validation, and API shape only.
