You are a backend engineering agent building Orbos. Read context.md and CLAUDE.md at the root before writing any code.
The Safety Agent and Lesson Designer are complete and generating real scripts. Today you build the Mastery Estimator — the agent that turns attempt data into mastery scores and drives the adaptive learning loop.

Step 1 — Mastery Estimator Agent
Create apps/api/src/agents/mastery/mastery-estimator.agent.ts:
typescriptexport type Recommendation = 'advance' | 'practice' | 'reteach';

export interface MasteryEstimate {
mastery_level: number;        // 0.0–1.0
confidence_score: number;     // 0.0–1.0
recommendation: Recommendation;
has_direct_lesson_attempt: boolean;
attempt_count: number;
reasoning: string;            // human-readable explanation, for the dashboard
}

export class MasteryEstimatorAgent {
estimate(input: {
student_id: string;
standard_id: string;
attempts: AttemptDto[];
}): Promise<MasteryEstimate>
}
Scoring algorithm — implement exactly as specified, document clearly for future IRT/BKT swap:
typescript// Base score per attempt
const score = attempt.correct ? 1.0 : 0.0;

// Penalties and bonuses
const hintPenalty = attempt.hint_used ? 0.3 : 0.0;
const speedBonus = (attempt.correct && attempt.time_spent_seconds < 10) ? 0.1 : 0.0;

// Source weighting
const sourceWeight = attempt.source === 'phenomenon' ? 0.8 : 1.0;

// Weighted attempt score
const attemptScore = (score - hintPenalty + speedBonus) * sourceWeight;
Then compute final mastery:

Take the weighted average of all attempt scores
Apply recency bias — more recent attempts count more (use exponential decay: most recent attempt has weight 1.0, previous has 0.9, one before that 0.8, etc.)
Clamp result to 0.0–1.0

Recommendation logic:

mastery_level >= 0.8 AND has_direct_lesson_attempt = true → advance
mastery_level >= 0.8 AND has_direct_lesson_attempt = false → practice (needs at least one direct lesson attempt before advancing)
mastery_level >= 0.5 → practice
mastery_level < 0.5 → reteach

Requirements:

This is pure TypeScript logic — no LLM call needed for the MVP algorithm
Add a comment block at the top of the file: // ALGORITHM: Weighted scoring v1. Replace this class with IRT or BKT implementation when ready. Interface must remain identical.
After computing the estimate, upsert the result into mastery_state table
Set has_direct_lesson_attempt = true if any attempt in the input has source === 'lesson'
The reasoning field should be a plain English summary: e.g. "4 attempts, 3 correct, 1 with hint. Recent performance improving. Ready to advance."


Step 2 — Attempt Logging Pipeline
Wire the full pipeline so logging an attempt triggers a mastery update.
Update apps/api/src/attempts/attempts.service.ts:
typescriptasync logAttempt(dto: LogAttemptDto): Promise<AttemptResponseDto> {
// 1. Insert attempt into attempts table
// 2. Fetch all attempts for this student + standard
// 3. Call MasteryEstimatorAgent.estimate()
// 4. Upsert result into mastery_state
// 5. Return the logged attempt
}
Update POST /attempts/log to call this pipeline. The route should now:

Accept a LogAttemptDto
Log the attempt
Trigger mastery recalculation
Return the attempt with the updated mastery level in the response:

typescriptexport class AttemptWithMasteryResponseDto {
attempt: AttemptResponseDto;
mastery: MasteryEstimate;
}

Step 3 — Mastery Route
Update GET /mastery/:studentId/:standardId to return real data from the mastery_state table. If no mastery state exists yet for that student+standard, return:
json{
"mastery_level": 0.0,
"confidence_score": 0.0,
"recommendation": "reteach",
"has_direct_lesson_attempt": false,
"attempt_count": 0,
"reasoning": "No attempts recorded yet."
}

Step 4 — Vitest Tests
Write thorough tests for the scoring algorithm — this is the heart of the adaptive system and must be correct:
typescriptdescribe('MasteryEstimatorAgent', () => {
it('returns 0.0 mastery with no attempts')
it('correct answer with no hint scores 1.0')
it('correct answer with hint scores 0.7')
it('incorrect answer scores 0.0')
it('phenomenon source applies 0.8 weight')
it('fast correct answer applies speed bonus')
it('recommendation is advance when mastery >= 0.8 with direct lesson attempt')
it('recommendation is practice when mastery >= 0.8 without direct lesson attempt')
it('recommendation is reteach when mastery < 0.5')
it('recency bias weights recent attempts higher')
it('mastery is clamped to 1.0 maximum')
it('mastery is clamped to 0.0 minimum')
})

Step 5 — Practice Generator Agent
This is simpler than the Lesson Designer — reuse most of the same pattern.
Create apps/api/src/agents/practice/practice-generator.agent.ts:
typescriptexport class PracticeGeneratorAgent {
generate(input: {
standard_id: string;
mastery_level: number;
student_age: number;
}): Promise<LessonScriptResponseDto>
}
Requirements:

Uses the same component library as the Lesson Designer
Prompt template name: practice_generator — seed it in the prompts table
Adapts difficulty based on mastery_level:

mastery_level < 0.5 → simpler questions, more story_card reinforcement
mastery_level 0.5–0.8 → mixed difficulty
mastery_level > 0.8 → challenge questions, less scaffolding


Must still start with story_card and end with confidence_check
Must still pass through Safety Agent before saving
Add route: POST /practice/generate with GeneratePracticeDto: { standard_id, mastery_level, student_age }


When you are done, confirm:

Log an attempt via the API and verify mastery updates:

bash# Log a correct attempt
curl -X POST http://localhost:3000/attempts/log \
-H "Content-Type: application/json" \
-d '{
"student_id": "<a real student id>",
"standard_id": "SEP-MAT-1-1.1",
"interaction_component": "multiple_choice",
"correct": true,
"time_spent_seconds": 8,
"hint_used": false,
"source": "lesson"
}'

Verify mastery updated:

bashcurl http://localhost:3000/mastery/<student_id>/SEP-MAT-1-1.1
Should return mastery_level > 0 and recommendation based on the scoring.

Log 4 more attempts with varying correctness and verify mastery changes appropriately.
All Vitest tests pass:

bashpnpm exec nx run api:test

POST /practice/generate returns a valid practice script adapted to mastery level.

Do not build the Learning Orchestrator today. That is Day 9. Today is Mastery Estimator, attempt pipeline, and Practice Generator only.
