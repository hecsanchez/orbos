You are a mobile engineering agent building Orbos. Read context.md and CLAUDE.md at the root before writing any code. Review the designs in /design before writing any component — match them exactly.
The first 3 components and the InteractionRenderer are complete. Today you finish all remaining MVP interaction components. After today every component needed for the pilot is built.
Standing rule for all components — read before writing any code:
typescript/**
* INTERACTION DESIGN PRINCIPLES — DO NOT OVERRIDE
* - Wrong answers: neutral animation only, never red
* - Always encouraging: TTS says "Inténtalo de nuevo" on wrong answer
* - No timers shown to child
* - No scores shown to child
* - Child can only move forward — no back navigation
    */

Component 1 — tap_reveal
Create apps/ipad/src/components/interactions/TapReveal.tsx:
typescriptinterface TapRevealProps {
prompt: string;           // shown before tap
tts_text_before: string;  // read on mount before tap
revealed_content: string; // shown after tap
tts_text_after: string;   // read after tap automatically
onComplete: () => void;
}
Requirements:

TTS reads tts_text_before on mount
Card shows prompt with a clear tap affordance (subtle pulse animation on the card)
Child taps anywhere on the card to reveal
Reveal animation: smooth flip or fade — match the design in /design
After reveal, TTS reads tts_text_after automatically
Continue button appears only after TTS finishes reading revealed content
This component never records correct/incorrect — always completes successfully
For 5yo: large illustrated card, minimal text, audio-first
For 8yo and 11yo: text + visual, cleaner layout


Component 2 — ordering
Create apps/ipad/src/components/interactions/Ordering.tsx:
typescriptinterface OrderingProps {
instruction: string;
tts_text: string;
items: { id: string; label: string; correct_position: number }[];
hint_text?: string;
onComplete: (result: { correct: boolean; hint_used: boolean; time_spent_seconds: number }) => void;
}
Requirements:

TTS reads tts_text on mount
Items are shown in shuffled order — never in correct order on mount
Use drag-to-reorder within a vertical list — check /design for whether tap-to-sequence or drag was recommended and implement that
While dragging: item lifts slightly with a shadow, other items shift to show insertion point
When child submits order (via "Comprobar" button):

All correct: soft green highlight on all items, advance after 1.5 seconds
Any wrong: neutral grey highlight on incorrect items only, gentle bounce, TTS says "Inténtalo de nuevo", child can retry


Hint button appears after 2 failed attempts — reads hint_text via TTS
Timer starts when items become interactive
Max 5 items — do not render more than 5 even if script provides more


Component 3 — confidence_check
Create apps/ipad/src/components/interactions/ConfidenceCheck.tsx:
typescriptinterface ConfidenceCheckProps {
question: string;         // e.g. "¿Qué tan seguro te sientes?"
tts_text: string;
mode: 'pre' | 'post';    // pre = before activity, post = after
onComplete: (result: { confidence: 1 | 2 | 3 }) => void;
}
Requirements:

TTS reads tts_text on mount
3 options presented as large tappable cards with icon + text:

1 → "No estoy seguro" + appropriate icon from /design
2 → "Más o menos" + appropriate icon from /design
3 → "¡Lo sé bien!" + appropriate icon from /design


No correct or incorrect — any selection is valid and advances
Selection highlights the chosen card softly, advances after 0.8 seconds
mode: 'pre' and mode: 'post' use the same UI — mode is stored in the attempt result for analysis only
Result is logged as an attempt with correct: true always — this is a self-report, not a test
For 5yo: icons larger than text, very simple icons
For 8yo + 11yo: icons and text balanced


Component 4 — match_connect
Create apps/ipad/src/components/interactions/MatchConnect.tsx:
typescriptinterface MatchConnectProps {
instruction: string;
tts_text: string;
left_items: { id: string; label: string }[];
right_items: { id: string; label: string; matches: string }[]; // matches = left item id
hint_text?: string;
onComplete: (result: { correct: boolean; hint_used: boolean; time_spent_seconds: number }) => void;
}
Requirements:

TTS reads tts_text on mount
Left column and right column rendered side by side
Right column items shuffled on mount — never in matching order
Interaction: child taps a left card (highlights it), then taps a right card
A line draws between them on connection — match the line style from /design
Correct match: line turns soft green, both cards dim and lock — cannot be unmatched
Wrong match: line dissolves with a gentle fade, both cards return to available — no negative feedback, no TTS reprimand
After 2 wrong attempts on the same left card: hint button appears
All pairs must be matched before onComplete fires
correct: true only if all matches are correct on first attempt per pair
Maximum 4 pairs — do not render more


Component Registry Update
Update apps/ipad/src/components/interactions/index.ts to include all 7 MVP components:
typescriptexport const INTERACTION_COMPONENTS = {
story_card: StoryCard,
multiple_choice: MultipleChoice,
drag_drop: DragDrop,
tap_reveal: TapReveal,
ordering: Ordering,
confidence_check: ConfidenceCheck,
match_connect: MatchConnect,
} as const;

Age Adaptation Utility
All components must adapt visually based on the child's age. Create apps/ipad/src/utils/age-theme.ts:
typescriptexport type AgeGroup = 'young' | 'middle' | 'older';

export function getAgeGroup(age: number): AgeGroup {
if (age <= 6) return 'young';
if (age <= 9) return 'middle';
return 'older';
}

export const AGE_THEME = {
young: {
fontSize: { instruction: 28, body: 24, button: 26 },
iconSize: 64,
showAudioButton: true,    // always visible
textDensity: 'minimal',
},
middle: {
fontSize: { instruction: 22, body: 18, button: 20 },
iconSize: 48,
showAudioButton: true,    // visible but smaller
textDensity: 'moderate',
},
older: {
fontSize: { instruction: 20, body: 16, button: 18 },
iconSize: 36,
showAudioButton: false,   // available on tap only
textDensity: 'full',
},
} as const;
Every component receives studentAge: number as a prop and uses getAgeGroup() to apply the correct theme. Update the existing 3 components (StoryCard, MultipleChoice, DragDrop) to use this utility as well.

Update InteractionRenderer
Update apps/ipad/src/components/InteractionRenderer.tsx to pass studentAge down to every component. The renderer receives studentAge from the session context and forwards it to whichever component is currently active.

When you are done, confirm:

All 7 components render correctly in the iOS Simulator on iPad target
Run through a full scripted lesson with all 7 component types — verify each renders, TTS fires, and completion is recorded
Test age adaptation — change student age from 5 to 11 and verify font sizes and icon sizes update
Verify no red appears anywhere in wrong answer flows — only neutral greys and soft animations
Component registry rejects unknown component names — test with a bad name:

typescript// Should throw a visible error, never silently skip
InteractionRenderer with script containing component: 'unknown_component'

Run typecheck:

bashpnpm exec nx run ipad:typecheck

Verify all 7 components are in the registry and no import errors:

bashpnpm exec nx run ipad:lint
Do not build the Session Engine today. That is D12. Today is the remaining 4 components and age adaptation only.
