you are a backend engineering agent building Orbos. Read context.md and CLAUDE.md at the root before writing any code.
The database is running with all 7 tables seeded with SEP standards. Your job today is to build three things: the LLMClient abstraction, the RAG index over SEP standards, and the validated JSON schemas for all 10 interaction components.

Step 1 — LLMClient
Create apps/api/src/llm/llm-client.ts. This is the single class all agents must use for LLM calls. No agent ever calls Anthropic or OpenAI SDKs directly.
typescript// apps/api/src/llm/llm-client.ts

export type LLMProvider = 'anthropic' | 'openai';

export interface LLMMessage {
role: 'user' | 'assistant' | 'system';
content: string;
}

export interface LLMOptions {
model?: string;
maxTokens?: number;
temperature?: number;
provider?: LLMProvider;
}

export class LLMClient {
// Default provider from env: LLM_PROVIDER=anthropic|openai
// Default models: claude-sonnet-4-5 | gpt-4o
generate(messages: LLMMessage[], options?: LLMOptions): Promise<string>
}
Requirements:

Install: pnpm add @anthropic-ai/sdk openai -w
Provider and model switchable via env vars LLM_PROVIDER and LLM_MODEL
If provider is not set, default to anthropic
Wrap both SDKs so callers get back a plain string — no SDK types leak outside this class
Add to .env.example: LLM_PROVIDER=anthropic and LLM_MODEL=claude-sonnet-4-5
Register as a NestJS injectable service in agents/agents.module.ts
Write Vitest unit tests in llm-client.spec.ts that mock both SDK calls and verify the output is a plain string


Step 2 — Prompt Template Service
Agents never hardcode prompt strings. Create apps/api/src/llm/prompt-template.service.ts:
typescriptexport class PromptTemplateService {
// Loads prompt by name from the DB prompts table
getTemplate(name: string): Promise<string>
// Renders template with variables using simple {{variable}} interpolation
render(template: string, variables: Record<string, string>): string
}
Create a prompts table migration:
sqlCREATE TABLE prompts (
id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
name varchar UNIQUE NOT NULL,
template text NOT NULL,
version integer NOT NULL DEFAULT 1,
created_at timestamp DEFAULT now()
);
Add it to apps/api/src/db/schema.ts and generate a new migration. Seed 3 placeholder prompt templates as stubs — one for each of the first agents you'll build on Days 8–10: lesson_generator, safety_checker, mastery_estimator.

Step 3 — Embeddings + RAG Index
Install:
bashpnpm add openai -w  # already installed if Step 1 is done
Create apps/api/src/rag/embeddings.service.ts:
typescriptexport class EmbeddingsService {
// Calls OpenAI embeddings API (text-embedding-3-small)
// Returns vector of length 1536
embed(text: string): Promise<number[]>

// Embeds all standards that have null embedding in DB
// Runs in batches of 100 to avoid rate limits
indexStandards(): Promise<{ indexed: number, skipped: number }>

// Given a query string, returns top-k most similar standards
search(query: string, topK?: number): Promise<Standard[]>
}
Requirements:

Use text-embedding-3-small (1536 dimensions, matches the vector column)
The search method uses pgvector's cosine similarity: embedding <=> query_vector
Add a Drizzle query helper for the vector similarity search
After building the service, run indexStandards() to populate all embeddings:

bashpnpm exec nx run api:seed-embeddings
Create that as a NestJS CLI script (not a route). Verify embeddings are populated:
bashpsql postgresql://orbos:orbos@localhost:5432/orbos \
-c "SELECT COUNT(*) FROM standards WHERE embedding IS NOT NULL;"
Count should match total standards count from Day 2.

Step 4 — Interaction Component Schemas
Create apps/api/src/interactions/schemas/ with one JSON schema file per component. All 10 components from context.md:
story_card.schema.json:
json{
"component": "story_card",
"version": 1,
"props": {
"title": { "type": "string", "maxLength": 60 },
"body": { "type": "string", "maxLength": 400 },
"tts_text": { "type": "string", "maxLength": 400 },
"continue_label": { "type": "string", "maxLength": 30 }
},
"required": ["title", "body", "tts_text"]
}
Define sensible schemas for all 10 components: story_card, tap_reveal, drag_drop, multiple_choice, ordering, build_object, slider, match_connect, audio_explain, confidence_check. Use the descriptions in context.md to infer the props each needs.
Create apps/api/src/interactions/interaction-validator.service.ts:
typescriptexport class InteractionValidatorService {
// Validates a single interaction block against its component schema
validate(interaction: { component: string, props: Record<string, unknown> }):
{ valid: boolean, errors: string[] }

// Validates a full interaction script array
validateScript(script: unknown[]):
{ valid: boolean, errors: string[] }
}
Requirements:

Rejects any component name not in the approved list — this is the anti-hallucination guard
Write Vitest tests covering: valid script passes, unknown component name rejected, missing required prop rejected, prop exceeding maxLength rejected


When you are done, confirm:

LLMClient.generate() works with both providers — test manually:

bashpnpm exec nx run api:test --testFile=llm-client.spec.ts

EmbeddingsService.search('suma de números') returns relevant Math standards
All 10 component schema files exist in interactions/schemas/
InteractionValidatorService.validate() rejects unknown component names
All Vitest tests pass:

bashpnpm exec nx run api:test
Do not build any agents today. That starts Day 8. Today is infrastructure only — LLMClient, RAG, schemas.
