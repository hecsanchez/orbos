# D18 — Batch Phenomenon Generation + Pilot Seeding

Sprint D24: Generate + approve 3 phenomenon proposals per student. Facilitation guides reviewed. Phenomena queued for pilot week.

## Changes

### API — Students Controller (fix)
- **Modified** `apps/api/src/students/students.controller.ts`
  - Replaced in-memory storage with Drizzle DB queries
  - Added `GET /students/:id` endpoint
  - `POST /students` now inserts into PostgreSQL

### API — Phenomena Batch Endpoints
- **Modified** `apps/api/src/phenomena/phenomena.controller.ts`
  - `POST /phenomena/batch-propose`: generates proposals for all students in DB
  - `PUT /phenomena/batch-approve`: approves all pending proposals at once
  - Returns summary with per-student success/error counts

### Pilot Seed Script
- **Created** `apps/api/src/db/seeds/seed-pilot-phenomena.ts`
  - 9 pre-crafted phenomenon proposals (3 per student)
  - Ana (5yo): leaf colors, animal legs, color mixing
  - Miguel (8yo): paper airplanes, dinosaur measurements, solar system
  - Sofia (11yo): terrarium ecosystem, geometric art, app design
  - All proposals inserted as `status: 'approved'` for pilot readiness
  - Idempotent — skips existing proposals
  - Run: `npx tsx apps/api/src/db/seeds/seed-pilot-phenomena.ts`

### Studio — Phenomena Page Enhancements
- **Modified** `apps/studio/src/app/phenomena/page.tsx`
  - "Generar para todos" button triggers batch generation
  - "Aprobar todos (N)" button approves all pending proposals
  - Evidence prompt now parses JSON for rich display (instruction + capture type)
- **Modified** `apps/studio/src/lib/api.ts`
  - Added `batchProposePhenomena()` and `batchApprovePhenomena()` API functions

## Verification
- `pnpm exec vitest run --config apps/api/vitest.config.mts` — 50 tests pass
- `pnpm exec nx run studio:build` — succeeds
- Students endpoint now returns DB data instead of empty in-memory array
- Batch propose generates 3 proposals per student
- Batch approve sets all pending → approved
- Seed script creates 9 approved proposals for pilot
